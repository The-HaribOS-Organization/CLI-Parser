#ifndef _COMMAND_
#define _COMMAND_

/*#include <string>  // temp

namespace commands
{
    class Command
    {
        public:
            Command() = delete;
            Command(const std::string& _name);
        private:
            std::string name;
    };

    // temp
    class CdCommand : public Command
    {
        using Command::Command;
        public:
            CdCommand() = delete;
            CdCommand(const std::string& _name);
        private:

    };

    // here put command utils
    
} // namespace commands
*/
#endif