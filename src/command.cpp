/*#include <iostream>
#include <string>

#include "../include/commands.hpp"

commands::Command::Command(const std::string& _name) : name(_name)
{
}

commands::CdCommand::CdCommand(const std::string& _name) : commands::Command(_name)
{
    // here put command code
}
*/